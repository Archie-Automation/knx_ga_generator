# Copy to .env and fill in. Used by: supabase functions serve
# Do not commit .env (add to .gitignore).

# From "stripe listen --forward-to ..." output (whsec_...)
STRIPE_WEBHOOK_SECRET=whsec_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Supabase – copy to .env and fill in your values.
# See: Supabase Dashboard → Project → Settings → API
#
# - Project URL  → VITE_SUPABASE_URL
# - anon public  → VITE_SUPABASE_ANON_KEY (gebruik de "anon" / "public" key, niet de service_role)
#
# Let op: "Database error saving new user" komt meestal door een trigger/fout in de database,
# niet door een verkeerde API key (verkeerde key geeft 401/403).
#
VITE_SUPABASE_URL=https://YOUR-PROJECT-REF.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Optional: enable [register-device] debug logs in release build
# VITE_DEBUG_REGISTER_DEVICE=true
